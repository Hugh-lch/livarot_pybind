```bash
sudo apt install build-essential gdb cmake pkg-config
sudo apt install libgsl-dev libglib-2.0-* libboost-all-dev libdouble-conversion-dev

pip install build
python -m build
pip install pylivarot-*.whl
```
